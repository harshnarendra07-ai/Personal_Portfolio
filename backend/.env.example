# Environment variables template
# Rename this file to .env and populate with your own credentials

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://johndoe:mypassword@localhost:5432/portfolio?schema=public"

# Nodemailer / Email settings
# Use either an App Password for Gmail or SMTP provider like sendgrid/resend
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Destination email for contact form submissions
CONTACT_EMAIL="harshnarendra07@gmail.com"

# Server Port Override (Defaults to 3000)
PORT="3000"